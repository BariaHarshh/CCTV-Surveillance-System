# 🧠 Machine Learning Models

Place YOLO object detection model weights (`.pt`) in this directory.

Currently required model:
- `yolov8n.pt` (Lightweight YOLOv8 Nano weights)

If missing, run the downloader script:
```bash
python scripts/download_models.py
```
